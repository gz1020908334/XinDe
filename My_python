1.再次导入
    
    from imp import *  先行导入
    reload((model))

    当model被修改以后需要重新导入用以上代码
2.import 路径问题
    import sys
    sys.path  'sys.path 是一个数组用来存放导入的查询路径'
    查看导入的路径
    sys.path.append('path')  path 是导入model的路径
3.循环导入
    解决办法
        用主模块来调用其他模块避免循环调用
4. == 与 is
    == 是判断值是否相等
    is 是指向（引用地址）是否相同
5.深拷贝与浅拷贝
    初步理解：
             浅拷贝只是拷贝引用
             深拷贝是另为创建对象
       对于不可变类型深浅拷贝地址值和拷贝后的值都是一样的
       可变类型
        =拷贝 值相等 地址相等
        copy浅拷贝 值相同 另外创建对象指向下层  所以地址不同 
        deepcopy深拷贝 值相同 地址不相同 对于每一层都创建了新的对象
